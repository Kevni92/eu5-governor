name: Run Governor Patch 0.2.11

on:
  push:
    paths:
      - '.github/scripts/patch_governor_0211.py'
      - '.github/workflows/run-governor-patch-0211.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply patch
        run: python .github/scripts/patch_governor_0211.py
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "Show governor confirmation costs correctly"
            git push origin HEAD:main
          fi
